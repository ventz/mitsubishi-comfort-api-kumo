# =============================================================================
# Kumo Cloud API Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.sample .env
#
# Then edit .env with your actual credentials.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Kumo Cloud Login Credentials
# -----------------------------------------------------------------------------
# Your Kumo Cloud account email and password (same as the mobile app)
KUMO_USERNAME=your@email.com
KUMO_PASSWORD=yourpassword

# -----------------------------------------------------------------------------
# OPTIONAL: Default Site ID
# -----------------------------------------------------------------------------
# Setting this avoids an extra API call when fetching device status.
# Get your site ID by running: python app.py raw sites
# Look for the "id" field in the output.
KUMO_SITE_ID=your-site-guid-here

# -----------------------------------------------------------------------------
# OPTIONAL: Device Serial Mapping (Friendly Names)
# -----------------------------------------------------------------------------
# Map friendly names to device serials for easier CLI usage.
# Pattern: KUMO_SERIAL_<NAME>=<serial>
#
# The <NAME> becomes the friendly name you use in commands (case-insensitive).
# Get device serials by running: python app.py raw zones
# Look for "deviceSerial" in the adapter section of each zone.
#
# Examples:
#   KUMO_SERIAL_BEDROOM=ABC123DEF456    -> use "bedroom" in commands
#   KUMO_SERIAL_LIVINGROOM=GHI789JKL    -> use "livingroom" in commands
#   KUMO_SERIAL_OFFICE=MNO012PQR        -> use "office" in commands
#   KUMO_SERIAL_MASTER=STU345VWX        -> use "master" in commands
#   KUMO_SERIAL_GUESTROOM=YZA678BCD     -> use "guestroom" in commands
#
# Then you can run commands like:
#   python app.py set-temp bedroom 72
#   python app.py set-mode office heat
#   python app.py set-fan livingroom quiet
#   python app.py status -v

# Uncomment and fill in your device serials:
# KUMO_SERIAL_BEDROOM=your-device-serial-here
# KUMO_SERIAL_LIVINGROOM=your-device-serial-here
# KUMO_SERIAL_OFFICE=your-device-serial-here
