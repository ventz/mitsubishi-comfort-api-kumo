[project]
name = "mitsubishi-comfort-api-kumo"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "httpx[http2]>=0.27.0",
    "python-dotenv>=1.0.0",
    "python-socketio[client]>=5.15.0",
    "websocket-client>=1.9.0",
]
